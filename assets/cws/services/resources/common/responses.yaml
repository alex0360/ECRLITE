components:
  responses:
    404:
      description: Operación fallida
      content:
        application/json:
          schema:
            $ref: 'schemas.yaml#/components/schemas/404'
    405:
      description: Método no permitido
      content:
        application/json:
          schema:
            $ref: 'schemas.yaml#/components/schemas/405'
    500:
      description: Operación fallida
      content:
        application/json:
          schema:
            $ref: 'schemas.yaml#/components/schemas/500'